---
const today = new Date();
---

<footer>
	<div class="footer-content">
		<div class="social-links">
			<a href="https://twitter.com/lastgoodhandle" target="_blank" rel="noopener">Twitter</a>
			<span class="separator">/</span>
			<a href="https://github.com/brettimus" target="_blank" rel="noopener">GitHub</a>
		</div>
		<p class="copyright">&copy; {today.getFullYear()} Brett Beutell</p>
	</div>
</footer>
<style>
	footer {
		padding: var(--space-2xl) var(--space-md);
		border-top: 1px solid var(--pale-birch);
	}
	.footer-content {
		max-width: var(--content-width);
		margin: 0 auto;
		text-align: center;
	}
	.social-links {
		display: flex;
		justify-content: center;
		align-items: center;
		gap: var(--space-sm);
		margin-bottom: var(--space-md);
	}
	.social-links a {
		font-family: var(--font-body);
		font-size: var(--font-size-sm);
		color: var(--warm-stone);
		text-decoration: none;
		transition: color var(--transition-base);
	}
	.social-links a:hover {
		color: var(--burnt-sienna);
	}
	.separator {
		color: var(--pale-birch);
	}
	.copyright {
		font-size: var(--font-size-sm);
		color: var(--warm-stone);
		margin: 0;
	}
</style>
