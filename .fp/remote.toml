# FP remote identity — managed by `fp project remote stamp`.
# Committed so sandboxes and CI resolve this project in REST mode.
# Contains no secrets: auth still comes from FP_TOKEN or credentials.toml.

project_id = "xlU5ij5ejhDTodSLWUttJ"
workspace_slug = "brettimus-LSnq"
server_url = "https://app.fp.dev"
prefix = "BBB"
